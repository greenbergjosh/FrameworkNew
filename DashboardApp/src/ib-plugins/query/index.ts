import { RegisterableComponent } from "@opg/interface-builder"
import layoutDefinition from "./layoutDefinition"
import QueryInterfaceComponent from "./QueryInterfaceComponent"

export default { component: QueryInterfaceComponent, layoutDefinition } as RegisterableComponent
