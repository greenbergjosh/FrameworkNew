import { RegisterableComponent } from "@opg/interface-builder"
import layoutDefinition from "./layoutDefinition"
import SelectInterfaceComponent from "./SelectInterfaceComponent"

export default { component: SelectInterfaceComponent, layoutDefinition } as RegisterableComponent
