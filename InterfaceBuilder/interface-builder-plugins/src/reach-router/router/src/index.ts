import layoutDefinition from "./layoutDefinition"
import { RegisterableComponent } from "@opg/interface-builder"
import RouterInterfaceComponent from "./RouterInterfaceComponent"

export default { component: RouterInterfaceComponent, layoutDefinition } as RegisterableComponent
