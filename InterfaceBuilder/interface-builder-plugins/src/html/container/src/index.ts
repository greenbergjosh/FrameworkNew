import layoutDefinition from "./layoutDefinition"
import { RegisterableComponent } from "@opg/interface-builder"
import ContainerInterfaceComponent from "./ContainerInterfaceComponent"

export default { component: ContainerInterfaceComponent, layoutDefinition } as RegisterableComponent
