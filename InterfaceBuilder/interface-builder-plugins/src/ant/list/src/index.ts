import layoutDefinition from "./layoutDefinition"
import { RegisterableComponent } from "@opg/interface-builder"
import ListInterfaceComponent from "./ListInterfaceComponent"

export default { component: ListInterfaceComponent, layoutDefinition } as RegisterableComponent
