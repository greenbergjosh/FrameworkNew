import layoutDefinition from "./layoutDefinition"
import { RegisterableComponent } from "@opg/interface-builder"
import CheckboxInterfaceComponent from "./CheckboxInterfaceComponent"

export default { component: CheckboxInterfaceComponent, layoutDefinition } as RegisterableComponent
