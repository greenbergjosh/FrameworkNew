import layoutDefinition from "./layoutDefinition"
import { RegisterableComponent } from "@opg/interface-builder"
import ColumnInterfaceComponent from "./ColumnInterfaceComponent"

export default { component: ColumnInterfaceComponent, layoutDefinition } as RegisterableComponent
