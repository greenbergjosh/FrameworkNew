import layoutDefinition from "./layoutDefinition"
import { RegisterableComponent } from "@opg/interface-builder"
import CardInterfaceComponent from "./CardInterfaceComponent"

export default { component: CardInterfaceComponent, layoutDefinition } as RegisterableComponent
