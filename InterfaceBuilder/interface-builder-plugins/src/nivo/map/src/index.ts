import layoutDefinition from "./layoutDefinition"
import { RegisterableComponent } from "@opg/interface-builder"
import MapInterfaceComponent from "./MapInterfaceComponent"

export default { component: MapInterfaceComponent, layoutDefinition } as RegisterableComponent
