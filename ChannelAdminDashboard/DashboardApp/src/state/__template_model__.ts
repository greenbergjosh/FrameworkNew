/**
 * copy pasta for when you need to create a new model
 * just replace __template_model__ with the name of your
 * own model as it's named when passed to Rematch.init()
 */
import * as Store from "./store.types"

// declare module "./store.types" {
//   interface AppModels {
//     __template_model__: {
//       state: State
//       reducers: Reducers
//       effects: Effects
//       selectors: Selectors
//     }
//   }
// }

// export interface State {}

// export interface Reducers {}

// export interface Effects {}

// export interface Selectors {}

// export const __template_model__: Store.AppModel<State, Reducers, Effects, Selectors> = {
//   state: {},

//   reducers: {},

//   effects: () => ({}),

//   selectors: () => ({}),
// }
